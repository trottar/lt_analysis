# KaonLT imported historical provenance

`chatgpt_project_history_2026-09-11/` is an immutable historical provenance
package. Its files preserve the original migration inputs, checksums, and
contracts; statements in them may have been current on 2026-09-11 but are not
current authority now. Do not edit imported files merely to update later state.

Consult [CURRENT.md](../CURRENT.md) for active state, current Git source/diff
for implementation, `evidence/` for accepted runtime evidence, `phases/` for
implementation/fix chronology, and `decisions/` for durable contracts.
Checksums inside the package protect historical provenance. This import is
navigation/provenance only and never active authority.
